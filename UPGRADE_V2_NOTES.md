# 第二阶段升级说明（v2.0.0）

## 本次新增 / 优化

**用户体验**
- KeepAlive 页面缓存：5 个 Tab + 股票详情 + 搜索页切换不销毁，滚动位置、K线周期/缩放、输入内容全部保留
- 统一行情 Store（Pinia marketStore）：首页/行情/自选共用一份数据、一个 10 秒轮询，消灭重复请求
- Stale-While-Revalidate：启动先显示本地缓存（localStorage market_cache_v1）秒开，后台刷新
- 骨架屏、错误重试块、空状态、Toast、确认对话框全套交互反馈
- 搜索页最近搜索（最多 8 条，可一键清除）
- 股票详情：更多数据折叠（振幅/均价）、底部固定 自选/卖出/买入 操作栏（适配 iPhone 安全区域）

**模拟交易（虚拟资金 100 万，可用环境变量 SIM_INITIAL_CASH 配置）**
- 新 Tab「交易」：总资产/今日盈亏/总收益/收益率/可用/市值/冻结 + 持仓列表（实时盈亏）
- 买入/卖出页：快捷比例 1/4、1/2、3/4、全仓(全部)，前端预校验 + 确认对话框
- 规则：买入 100 股整数倍(40009)、资金不足(40007)、超可卖(40008)，按当前行情价立即成交
- 成交记录 + 资金流水；下单五步（扣资金/持仓/订单/成交/流水）同一数据库事务，FOR UPDATE 行锁防并发
- 后端新增 5 表、5 实体、5 Mapper、SimulationService、/api/sim/** 接口（需登录）

**设置**
- 主题模式：跟随系统（matchMedia 实时监听）/ 浅色 / 深色
- 主题色：蓝/紫/绿/橙；涨跌颜色：红涨绿跌 / 绿涨红跌 —— 全部抽象为 CSS 变量，图表运行时读取
- 清除缓存：只清行情缓存与最近搜索，不动登录/自选/交易数据
- 全部设置持久化到 localStorage（app_settings_v1）

## 部署升级步骤

1. **数据库**：在 TablePlus 执行 `database/upgrade_simulation.sql`（5 张新表，幂等可重复执行；手机版建议按表逐块粘贴执行）
2. **后端**：代码推 GitHub → Render 自动重新部署（可选环境变量 `SIM_INITIAL_CASH=1000000`）
3. **前端**：推 GitHub → Cloudflare 自动重建（`.env.production` 里的 `VITE_API_BASE_URL` 保持不变）

## 注意

- 本次开发环境无法联网真实编译，已做全量静态校验（80 个 Java 文件结构校验、34 个 TS/Vue 文件语法校验均通过）。如 Render 构建报错，把日志发我即可快速定位。
- 原有功能（行情/分时/K线/指标/新闻/登录/自选）代码路径未破坏，只做了页面接入 Store 的改造。
